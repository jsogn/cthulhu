# Cthulhu · 研究实验台定位与扩展架构

> 文档导航：[README](../README.md) · [文档索引](README.md) · [研究用途与合规声明](研究用途与合规声明.md) · [实施文档](实施文档.md) · [发布打包](发布打包.md) · [界面导览](界面导览.md)

> **研究用途声明**：本文仅用于数字水印、内容指纹、媒体取证与视频变换的研究、
> 教学和授权测试。严禁用于规避平台规则、去除版权/溯源水印、侵权搬运、账号
> 矩阵规避或其他违法违规用途。完整边界见
> [研究用途与合规声明](研究用途与合规声明.md)。

> 2026-08-21 定位修订：项目核心为「数字水印与内容指纹研究实验台」，当前两个
> 主力研究方向是内容指纹相似度分析与数字水印鲁棒性变换，后续按需扩展研究能力；
> 本文固化研究定位、设计哲学与扩展缝隙，供后续实验按统一方式接入。
>
> 2026-08-24 口径收口：检测定位为「参考/自检」，不承诺识别平台暗水印；
> 研究价值主张 = 基准库鲁棒性评估 + 内容变换实验 + 授权环境下的合规性观察。
> 任何平台反馈都不得被用于规避平台规则。

## 一、研究定位

- 名称：Cthulhu
- 场景：数字水印、内容指纹、媒体取证与视频变换的研究、教学和授权测试
- 当前能力：素材库、参考检测（研究口径）、变换实验、相似度分析、批量任务与
  历史记录；所有能力均限于合成样本、自有素材或已授权素材
- 远期候选：公开数据集适配、可复现实验管理、差分分析方法、鲁棒性基准扩展等
  （未定，仅预留）

## 二、设计哲学

1. 观感与可复现性并重：研究变换应尽量保持内容可用，并如实记录画质代价。
2. 证据驱动：任何结论必须过代理回归门；平台反馈只能在授权范围内解释。
3. 离线私有：实验素材不出本机，不依赖云服务或按次计费。
4. 流程简单、参数有边界：默认配置用于基准实验，极端配置仅限隔离研究。
5. 合规优先：任何与 [研究用途与合规声明](研究用途与合规声明.md) 冲突的用途
   均不得执行。

## 三、扩展缝隙（新增功能应走的既有接口）

| 扩展点 | 位置 | 接入方式 |
|---|---|---|
| 新处理类型 | `jobs.RUNNERS` + `TaskSpec.kind` | 新增 runner 函数与 kind 枚举，入队即可用 |
| 新前端视图 | `ui/src/views/` + `Rail` 导航 | 新建视图组件并登记导航项 |
| 新变换策略 | `transform/strategies.py` | `register_strategy` 注册，按名称切换 |
| 新代理/模型 | `fingerprint/` + `deep.py` 样例 | ONNX Runtime 惰性加载，缺失自动降级 |
| 素材元数据 | `db.py` materials/reports | 后续加 tags/分组字段时迁移预留 |
| 设置与模板 | settings 键值 + templates payload | 参数化扩展，无需改表结构 |
| 进度事件 | `events.broker` + 任务队列 | 新任务复用进度/取消/暂停协议 |

约定：新处理类型必须复用任务队列（排队/并行/进度/取消/持久化），新算法
必须挂进回归门（`fingerprint gate`），新模型必须支持缺失降级，不把重依赖
（如 PyTorch）打进研究构建。
