{
  "name": "cthulhu",
  "version": "0.6.3",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "dev": "VITE_AUTH_TOKEN=dev-local CTHULHU_AUTH_TOKEN=dev-local concurrently -k -n ui,backend -c blue,green \"pnpm --dir ui dev\" \"uv run --project backend --extra purify uvicorn cthulhu_backend.main:app --port 57173 --reload --reload-dir backend/src\"",
    "dev:desktop": "VITE_AUTH_TOKEN=dev-local CTHULHU_AUTH_TOKEN=dev-local concurrently -k -n ui,electron -c blue,magenta \"pnpm --dir ui dev\" \"pnpm --dir electron dev\"",
    "build": "pnpm --dir ui build",
    "typecheck": "pnpm --dir ui typecheck",
    "test": "pnpm --dir ui test && pnpm --dir electron test",
    "package:model": "uv run --project backend --extra purify python packaging/fetch_purify_model.py",
    "package:backend": "pnpm package:model && uv run --project backend --extra purify pyinstaller packaging/cthulhu-backend.spec --distpath packaging/backend-dist --workpath packaging/build --noconfirm",
    "package": "pnpm build && pnpm package:backend && pnpm --dir electron dist"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
